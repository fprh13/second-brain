---
created: 2026-03-15 03:27:29
---
# AGENTS.md

## 목적

이 저장소는 개인 지식 관리를 위한 Obsidian 볼트입니다.

사용자의 역할은 구조, 가독성, 일관성을 유지하면서 노트의 생성, 정제, 정리를 돕는 것입니다.

기본 정책 문서로 `docs/vault-rules.md`를 따릅니다.

---

## 핵심 규칙

- 사용자가 명시적으로 요청하지 않으면 노트 파일을 이동하지 않습니다.
- 노트 위치는 사용자가 결정합니다.
- 치명적인 오타 수정 외에는 사용자 내용을 재작성하지 않습니다.
- 어떤 노트의 의미나 의도를 바꾸지 않습니다.
- 수정은 최소 범위로, 정확하고 되돌리기 쉽게 유지합니다.

---

## 편집 규칙

- 기존 `##` 섹션 사이로 내용을 이동하지 않습니다.
- 노트의 원래 구조를 유지합니다.
- 변경은 가독성 개선에만 한정합니다.
- 과도한 포맷팅이나 문체 위주의 재작성을 피합니다.
- 명령어, 설정, 로그, 코드는 fenced code block을 사용합니다.

---

## 볼트 구조 규칙

다음 볼트 구조를 존중합니다.

- `00 Inbox` → 임시 노트
- `01 Permanent` → 상시 지식 노트
- `02 MOCs` → 맵 오브 콘텐츠
- `10 Projects` → 진행 중인 작업
- `11 Areas` → 지속 책임 영역
- `12 Resources` → 참고 자료
- `13 Archive` → 비활성 자료
- `14 Templates` → 재사용 가능한 템플릿
- 폴더 이름을 변경하지 않습니다.
- 폴더 구조를 재정리하지 않습니다.

---

## 링크 규칙

- 내부 링크는 `[[Note Name]]` 형식을 사용합니다.
- 노트마다 `related notes` 섹션은 하나만 유지합니다.
- 과도하거나 불필요한 링크를 피합니다.
- 그래프 가독성을 해치거나 연결이 꼬이지 않도록 유지합니다.

---

## 첨부 규칙

- 첨부 파일은 `attachments/` 아래에 저장합니다.
- `diagrams/`, `screenshots/`, `files/`로 구분해 정리합니다.
- 모든 첨부 참조가 유효한지 확인합니다.

---

## 템플릿 규칙

- 최소 필수 frontmatter만 유지합니다. (`created`, 노트 유형 태그)
- 템플릿을 갱신할 때는 오래된 메타데이터를 제거합니다.
- 불필요한 필드를 추가하지 않습니다.

---

## 검증 규칙

- 내부 링크가 올바르게 해석되는지 확인합니다.
- 중복된 노트 파일명이 있는지 확인합니다.
- 위치가 부적절한 노트는 교정안을 제안하되 자동 이동하지 않습니다.
- 첨부가 올바르게 참조되는지 확인합니다.
- 노트 전반의 일관성을 유지합니다.

---

## 커밋 규칙

- 커밋 메시지는 항상 `notes: sync`를 사용합니다.
- 임의의 커밋 메시지를 만들지 않습니다.
- 모든 변경에서 커밋 메시지 규칙을 일관되게 유지합니다.

---

## Pull Request 규칙

- 목적, 변경 경로, 마이그레이션 메모를 포함합니다.
- Obsidian UI 동작에 영향이 있을 때만 스크린샷을 포함합니다.
- 필요하다면 백링크나 그래프 구조 관련 위험을 명시합니다.

---

## 언어 규칙

- 노트가 의도적으로 영어로 작성된 경우가 아니라면 기본 언어는 한국어입니다.

---

## 안전 규칙

- 파괴적인 작업은 절대 수행하지 않습니다.
- 명시적 지시 없이 볼트를 재정리하지 않습니다.
- 확신이 없으면 임의로 진행하지 말고 확인합니다.
